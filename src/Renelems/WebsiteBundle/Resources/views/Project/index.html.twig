{% extends 'RenelemsWebsiteBundle::layout.html.twig' %}

{% block content %}
    <section>
        <h1 class="blue">Projecten</h1>

        {% for project in aProject %}
        <h2>{{ project.title }}</h2>
        <p>{{ project.introduction }}</p>
        <div class="text-right">
            <a href="" class="btn blue">naar project</a>
        </div>
        {% endfor %}
    </section>
{% endblock %}
